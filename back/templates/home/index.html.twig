{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
	<h2>Apply Now</h2>
	<hr/>
	{{ form_start(form)}}
	{{ form_widget(form)}}
	{{ form_errors(form) }}
	<button type="submit" class="btn btn-info">Apply</button>
	{{ form_end(form)}}

{% endblock %}
